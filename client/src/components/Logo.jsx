import logo from "../assets/images/logo.svg";

// Displays the application's logo, rendering it as an image.

const Logo = () => {
  return <img src={logo} alt="jobify" className="logo" />;
};

export default Logo;
